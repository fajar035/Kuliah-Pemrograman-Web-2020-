syntax git wajib hafal 
1. git status (cek file, masih merah atau hijau, kalau merah git add)
2. git add . (. = semua file)
3. git commit -m 'isi pesan' (update ke git file dan kirim pesan / -m = message)
4. git push (upload file ke github / login akun github dulu)

…or create a new repository on the command line
echo "# VSGA_Perpustakaan" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:ikehikeh151/VSGA_Perpustakaan.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin git@github.com:ikehikeh151/VSGA_Perpustakaan.git
git branch -M main
git push -u origin main